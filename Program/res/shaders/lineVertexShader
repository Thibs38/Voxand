#version 400 core

in vec3 position;
in vec3 color;

uniform mat4 projectionViewMatrix;

out vec3 lineColor;

void main(void)
{
  gl_Position = projectionViewMatrix * vec4(position,1.0);
  lineColor = color;
}